global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: ingestion
    metrics_path: /metrics
    static_configs:
      - targets: ["ingestion:8080"]

  - job_name: processing
    metrics_path: /metrics
    static_configs:
      - targets: ["processing:8081"]
